name: Python Demos

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/python-demos.yml'
      - 'demo/python/**'
      - '!demo/python/README.md'
      - 'resources/audio_samples/**'
  pull_request:
    branches: [main, 'v[0-9]+.[0-9]+']
    paths:
      - '.github/workflows/python-demos.yml'
      - 'demo/python/**'
      - '!demo/python/README.md'
      - 'resources/audio_samples/** '

defaults:
  run:
    working-directory: demo/python

jobs:
  build-github-hosted:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ['3.7', '3.8', '3.9', '3.10']

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Pre-build dependencies
      run: python -m pip install --upgrade pip

    - name: Build dependencies
      run: |
        pip install wheel && cd ../../binding/python && python setup.py sdist bdist_wheel && pip install dist/pveagle-1.0.0-py3-none-any.whl

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Test enroll
      run: >
        python eagle_demo_file.py
        --access_key "/HZMi794/9p1aXKc3AsxHQIEQoV/Mf3/L4tHhHMXRtE7OJY64iJXxA=="
        enroll
        --enroll_audio_paths ../../resources/audio_samples/enroll_1.wav ../../resources/audio_samples/enroll_2.wav
        --profile_output_path test.egl

    - name: Test inference
      run: >
        python eagle_demo_file.py
        --access_key "/HZMi794/9p1aXKc3AsxHQIEQoV/Mf3/L4tHhHMXRtE7OJY64iJXxA=="
        test
        --profile_input_paths test.egl
        --test_audio_path ../../resources/audio_samples/test.wav